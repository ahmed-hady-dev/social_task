import 'package:flutter/material.dart';

class MomentsView extends StatelessWidget {
  const MomentsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MomentsView'),
      ),
    );
  }
}
